{
  "name": "agent-session-bridge",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "description": "Manage local Codex, Claude Code, and Gemini CLI sessions with tmux, a web UI, CLI, HTTP API, and optional Feishu integration.",
  "type": "module",
  "homepage": "https://github.com/otterview-labs/agent-session-bridge#readme",
  "bugs": {
    "url": "https://github.com/otterview-labs/agent-session-bridge/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/otterview-labs/agent-session-bridge.git"
  },
  "keywords": [
    "claude-code",
    "codex-cli",
    "gemini-cli",
    "self-hosted",
    "sqlite",
    "tmux",
    "typescript"
  ],
  "os": [
    "darwin",
    "linux"
  ],
  "packageManager": "npm@11.8.0",
  "engines": {
    "node": ">=22.19.0"
  },
  "bin": {
    "agent-session-bridge": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "dev": "tsx src/cli.ts",
    "dev:server": "tsx src/server.ts",
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.test.json",
    "start": "node dist/cli.js",
    "start:server": "node dist/server.js",
    "check": "npm run typecheck && npm test && npm run clean && npm run build"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.86.0",
    "@earendil-works/pi-ai": "0.86.0",
    "@larksuiteoapi/node-sdk": "^1.73.1",
    "diff": "^9.0.0",
    "isomorphic-git": "^1.41.9",
    "pino": "^9.9.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
